logging:
  config: classpath:logback-spring.xml

spring:
  mvc:
    view:
      prefix: /WEB-INF/views/jsp/
      suffix: .jsp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/coin_demo?serverTimezone=UTC&useSSL=false
    username: root
    password: '@2021sUPERmAN'

apiUrl: 'https://api.coindesk.com/v1/bpi/currentprice.json'
